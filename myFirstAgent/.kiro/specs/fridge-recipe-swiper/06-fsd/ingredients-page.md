# 食材页面规格 (Ingredients Page)

## 页面概述

展示 AI 识别的食材结果，允许用户修正和补充。

## 线框图

```
┌─────────────────────────────────┐
│  🍳 冰箱食谱              ❤️   │
├─────────────────────────────────┤
│                                 │
│  识别结果                       │
│  AI 已识别以下食材，可手动调整   │
│                                 │
│  ┌───────┐ ┌───────┐ ┌───────┐ │
│  │番茄 ×│ │鸡蛋 ×│ │青椒 ×│ │  ← 食材标签
│  └───────┘ └───────┘ └───────┘ │
│  ┌───────┐ ┌───────┐           │
│  │洋葱 ×│ │大蒜 ×│           │
│  └───────┘ └───────┘           │
│                                 │
│  ┌─────────────────────┐ ┌───┐ │
│  │ 添加其他食材...      │ │添加│ │  ← 添加输入
│  └─────────────────────┘ └───┘ │
│                                 │
│  ┌─────────────────────────┐   │
│  │    生成食谱推荐 ✨       │   │  ← 主按钮
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

## 组件结构

```
IngredientsPage
├── PageTitle
├── IngredientList
│   └── IngredientTag (× n)
├── AddIngredient
│   ├── Input
│   └── Button
└── Button (生成食谱)
```

## 组件规格

### IngredientTag

| 属性 | 类型 | 说明 |
|------|------|------|
| name | `string` | 食材名称 |
| onRemove | `() => void` | 删除回调 |
| confidence | `number?` | AI 置信度 (可选显示) |

### AddIngredient

| 属性 | 类型 | 说明 |
|------|------|------|
| onAdd | `(name: string) => void` | 添加回调 |
| placeholder | `string` | 输入框占位符 |

## 状态

```typescript
const ingredients = useAppStore(state => state.ingredients);
const addIngredient = useAppStore(state => state.addIngredient);
const removeIngredient = useAppStore(state => state.removeIngredient);
const setStep = useAppStore(state => state.setStep);

// 本地状态
const [inputValue, setInputValue] = useState('');
```

## 交互流程

```
1. 显示 AI 识别的食材标签
   ↓
2. 用户可点击 × 删除误识别的食材
   ↓
3. 用户可在输入框添加新食材
   ↓
4. 点击「生成食谱推荐」
   ↓
5. 进入 Loading → 跳转滑动页
```

## 事件处理

```typescript
const handleRemove = (index: number) => {
  removeIngredient(index);
};

const handleAdd = () => {
  if (inputValue.trim()) {
    addIngredient(inputValue.trim());
    setInputValue('');
  }
};

const handleGenerate = async () => {
  if (ingredients.length === 0) {
    alert('请至少添加一个食材');
    return;
  }
  setStep('loading');
  try {
    const recipes = await generateRecipes(ingredients);
    setRecipes(recipes);
    setStep('swipe');
  } catch (error) {
    setStep('ingredients');
  }
};
```

## 样式要点

- 标签：白底圆角，带阴影
- × 按钮：红色圆形
- 输入框：圆角边框，聚焦时高亮
- Flex wrap 排列标签
